// Thin compatibility barrel — the real implementations live in data-table-state-table.tsx.
// Kept so that any existing deep import path continues to resolve.
export {
  DataTableEmptyRender,
  DataTableErrorRender,
  DataTableNoResultsRender,
} from './data-table-state-table'
